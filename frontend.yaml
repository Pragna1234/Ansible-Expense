- name: configure frontend server
  become: yes
  hosts: frontend
  vars:

  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest
  - name: start and enable nginx
    ansible.builtin.service:
      name: nginx
      state: start
      enabled: yes


